<UserControl x:Class="STKMonitor.UI.UserControls.ArmActivity"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:STKMonitor.UI.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Canvas Name="actCanvas">
            
        </Canvas>
        <!--<ListView ItemsSource="{Binding Behaviours}" Name="asd">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding IsDone}"></TextBlock>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
        </ListView>-->
    </Grid>
</UserControl>
